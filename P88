#include <math.h>
#include <stdio.h>
double findSQRT(double N) {
    return sqrt(N); 
}
int main()
{
    int N;
   printf("enter the number:");
   scanf("%d",&N);
    printf("%f ", findSQRT(N));
    return 0;
}
